#include "board.h"

bitset<64> legalSquares(bitset<64> compound, bitset<64> enemy, bitset<64> pieceHome, pieceType p){
    switch (p)
    {
    case PAWN:
        
        break;
    
    default:
        break;
    }
}